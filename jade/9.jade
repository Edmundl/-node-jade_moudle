html
	head
	body
		-for(var i=0;i<arr.length;i++)
			div=arr[i]
		div!=content
		
		-var a=15;
		if(a%2==0)
			div(style={background:'red'}) 偶数
		else
			div(style={background:'green'}) 奇数
			
		-var b=2;
		case b
			when 0
				div aaa
			when 1
				div bbb
			when 2
				div ccc
			default
				|不靠谱